# AIVIDO V1 — FINAL RELEASE INTEGRATION GATE

- **Release branch:** `aivido/v1-release`
- **Integration:** `aivido/v1-productization` @ `9811a533580c10956748772cb0b828e1698b0714`
  + `aivido/v1-final-defect-closure` @ `c080464b80ea3afc975219def581dee60b95981b`
- **Integration shape:** linear (closure is a direct descendant of productization on the
  RC1.1 base `736042d`) — clean fast-forward integration, zero conflicts, nothing merged away.
- **Release HEAD:** `c080464b80ea3afc975219def581dee60b95981b`
- **Date:** 2026-09-07 · Host: shadow-6kckcfdq (Windows, Python 3.13.15, UE 5.8.2)

## 1. No lost Productization functionality (9811a53)

| Capability | Evidence at gate | Verdict |
|---|---|---|
| Installer | `install-aivido.ps1` ran end-to-end in the release tree: fresh `.venv`, requirements install, import contract validation, Unreal build probe (WARN-if-absent by design), project select, persistent backend start, UI handoff → `AIVIDO V1 INSTALL OK` | PASS |
| Persistent backend | `aivido_runtime.py` detached spawn on 127.0.0.1:8765; state/pid on disk; duplicate/foreign-listener protection; survives launcher exit (doctor `persistence` check PASS) | PASS |
| Watchdog | `aivido_watchdog.py` spawned per service (bounded restarts: 3 per 15 min); alive alongside backend in every status probe | PASS |
| start/stop/restart | doctor `stop_start_cycle` PASS (stop released port, start healthy, single listener); `restart` command verified | PASS |
| Tailscale | `remote on` → `tailscale serve --bg` → `https://shadow-6kckcfdq.tail5f3ac6.ts.net` proxying 127.0.0.1:8765; remote `/app` HTTP 200 | PASS |
| Doctor | `scripts/aivido_doctor.py` full mode: **PASS, 13/13 checks, 0 warn, 0 fail** (release SHA ancestry, python, requirements, backend health, UI, bridge 6766, Unreal identity, AividoHQ map, proof endpoint, duplicate start, stop/start cycle, persistence, foreign protection) | PASS |
| Package builder | `scripts/build_v1_package.py` → `dist/Aivido-V1` (132 files, version.json pinned to this branch/SHA) | PASS |

## 2. No lost defect-closure functionality (c080464)

| Capability | Evidence at gate | Verdict |
|---|---|---|
| Explicit verification requirements execute | strict read-only mission planned one real step per explicit check: 7× `verify_scene` + `capture_unreal_viewport` (+`inspect_project`, `unreal_ping`), 10/10 executed | PASS |
| False PASS impossible | verdict gate held: PASS only with every requirement planned+executed+evidenced; the gate's own run returned `verdict=PASS` with all 8 checks evidenced (and the closure's live false-PASS proof — `UNVERIFIED_REQUIREMENTS` FAIL path — remains in `tests/test_verification_mission.py`, 20 hermetic tests, all green) | PASS |
| Evidence objects render correctly | per-requirement evidence entries returned structured (`kind/check/measured/expected/ok/detail`); UI `fmtEvidence()` shipped and covered by the suite | PASS |

## 3. Full safe suite (ONE run)

`pytest tests/` on the release tree → **1082 passed / 1 skipped / 0 failed** (133.9 s).
Exact expected baseline. Imports verified to resolve to the release tree
(`app/served.py`, `core/mission.py` under `release-v1`).

## 4. Doctor

**PASS** — 13 PASS / 0 WARN / 0 FAIL (full mode, JSON in gate evidence).

## 5. Real live Unreal E2E (Create → Modify → Verify → Capture → Cleanup)

`scripts/aivido_smoke.py` against the live editor bridge (6766, UE 5.8.2, map
`/Game/Maps/AividoHQ.AividoHQ`, baseline 166 actors):

- Create: `spawn_cube` PASS (StaticMeshActor, mesh loaded, in-view spawn point)
- Modify: `actor_moved` PASS (readback == target), `actor_scaled` PASS (3,3,3), `modify_readback` PASS (independent get_actor)
- Verify: `actor_exists` PASS, `only_smoke_actor_added` PASS (exactly one actor added)
- Capture: `viewport_aimed` PASS (engine look-at), `viewport_capture` PASS (fresh PNG, NativeEditorViewport), `proof_saved` PASS
- Cleanup: `actor_deleted` PASS, `actor_absent_after` PASS, `level_restored` PASS (166 == 166 baseline), `no_save_performed` PASS (level intentionally not saved)

**SMOKE OVERALL: PASS — 18/18 steps.** Evidence: `reports/release/AIVIDO_V1_SMOKE_EVIDENCE/`
(fresh `AIVIDO_V1_INSTALL_SMOKE_proof.png` + `mission.json`, regenerated by this gate run).

## 6. Strict read-only AividoHQ verification

Verbatim prompt via `POST /api/unreal-coder` (`read_only: true`) — the Aivido UI path.
**Verdict: PASS — 10/10 steps, every requirement planned, executed and evidenced.**

| Requirement | Measured | Expected | ok |
|---|---|---|---|
| bridge healthy | healthy (UNREAL_BRIDGE_READY) | healthy | ✅ |
| map AividoHQ | `/Game/Maps/AividoHQ.AividoHQ` | AividoHQ | ✅ |
| human agents | **8** (`AVIDO_Human` prefix) | 8 | ✅ |
| props | **23** (`W3I_` prefix) | 23 | ✅ |
| movable lights | **41** (static 0, stationary 0, unclassified 0) | 41 | ✅ |
| missing skeletal meshes | **0** among 8 checked | 0 | ✅ |
| missing prop mesh/material references | **0** null among 23 checked | 0 | ✅ |
| fresh viewport proof | fresh PNG (30,197 bytes) captured this run | capture | ✅ |

## 7. UI reachability

| Surface | Result |
|---|---|
| Local `http://127.0.0.1:8765/app` | HTTP 200 (21,711 bytes, Aivido UI marker) |
| Tailscale `https://shadow-6kckcfdq.tail5f3ac6.ts.net/app` | HTTP 200 (identical page) |
| Tailscale IP `http://100.84.156.24:8765/app` | HTTP 200 (identical page) |

## 8–9. Final dist/Aivido-V1 + junk scan

`dist/Aivido-V1` built from this exact HEAD (`version.json` → branch `aivido/v1-release`,
SHA `c080464b…`). Scan of all **132 files**:

- junk (caches, backups, `*.zip`, broken-encoding, logs, pids, venvs): **NONE**
- secrets (key/pem/env material, private-key blocks, generated API keys, AWS ids): **NONE**
- top level: installer `.ps1/.cmd`, start/stop, `README-QUICKSTART.md` (3 actions),
  `requirements.txt`, `app/ core/ tools/ blender_agent/ ui/ memory/ config/settings.json`,
  runtime/watchdog/doctor/smoke/install-check scripts, `version.json`

The RC1.1 packaging-hygiene defect (tracked `ui/` junk) is confirmed fixed by the
package builder's UI exclude list (`backup-`, `.zip`, `.broken-encoding`).

## 10. Persistence evidence at gate

Backend RUNNING across tool-call boundaries in this environment via scheduled-task
spawn (pid 6872 + watchdog 8628, health OK, restarts counter healthy); the runtime's
own detached-spawn persistence was additionally proven by the doctor's
`persistence` check (launcher exited rc=0, backend stayed healthy) and by the
productization lane's scheduled-task certification.

## Warnings (non-blocking)

1. **Sandbox artifact (not a product defect):** in-agent-tool-call spawned process
   trees can be reaped at tool-call boundaries. The doctor's stop/start cycle left a
   probe-spawned backend that was later reaped; the live gate used the certified
   scheduled-task spawn. Product runtime behavior (detached spawn + watchdog) is
   unchanged and PASS.
2. Smoke `dirty_flag=true` is transient in-editor state from the disposable actor;
   the level is deliberately never saved, so nothing is persisted.
3. Pre-existing cosmetic warnings from the suite: duplicate OpenAPI operation IDs
   (unreal_coder/camera registrations) and FastAPI `on_event` deprecations. No
   behavior impact.
4. `restarts: 1` in runtime state is the cumulative counter carried from the doctor's
   stop/start cycle — not a crash count.

## Verdict

- critical: **0**
- major: **0**
- warnings: 4 (all non-blocking, itemized above)

**AIVIDO_V1_RELEASE_READY: YES**
